import 'package:flutter/material.dart';

class InteresCompuestoView extends StatelessWidget {
  const InteresCompuestoView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Interés Compuesto'),
      ),
      body: const Center(
        child: Text('Contenido de la página de Interés Compuesto'),
      ),
    );
  }
}